# Development Assets

This directory holds source atlases, old preview renders, and reference art that should not be served or copied into production builds.

Runtime assets stay under `public/assets`. Asset-generation scripts can read from `dev-assets/source` and write optimized output back to `public/assets`.
